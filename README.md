# fundamentals-of-AIML
The Daily Task Organizer (DTO) is a command-line interface (CLI) tool that helps users manage their daily tasks. It uses a text file on your computer as a simple database to store tasks, which can be Added, Viewed, Marked as Complete, and Deleted. This solves the common problem of forgotten tasks and disorganization.
